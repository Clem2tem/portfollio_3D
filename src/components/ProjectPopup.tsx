import React from 'react'

// ProjectPopup was inlined into UI.tsx. Export a harmless noop component to avoid accidental imports.
const ProjectPopup: React.FC<any> = () => null

export default ProjectPopup
